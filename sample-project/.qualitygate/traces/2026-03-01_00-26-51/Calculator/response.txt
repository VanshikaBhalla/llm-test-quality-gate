 Here is the JUnit 5 test class for the Calculator class with the specified rules:

```java
package com.example;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

public class CalculatorTest {
    private final Calculator calculator = new Calculator();

    @Test
    public void testAdd() {
        assertEquals(5, calculator.add(2, 3));
        assertEquals(-1, calculator.add(-2, 3));
        assertEquals(0, calculator.add(-2, -3));
        assertThrows(IllegalArgumentException.class, () -> calculator.add(Integer.MIN_VALUE, 1));
    }

    @Test
    public void testDivide() {
        assertEquals(2, calculator.divide(4, 2));
        assertEquals(-2, calculator.divide(4, -2));
        assertThrows(ArithmeticException.class, () -> calculator.divide(4, 0));
        assertThrows(IllegalArgumentException.class, () -> calculator.divide(Integer.MIN_VALUE, 1));
    }
}
```

This test class includes tests for the `add()` and `divide()` methods in the Calculator class. The `assertEquals()` method is used to verify that the expected results match the actual results returned by the calculator. The `assertThrows()` method is used to verify that exceptions are thrown correctly when invalid input is provided.