 Here is a JUnit 5 test class for the `Calculator` class with the specified rules:

```java
package com.example;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

public class CalculatorTest {

    private final Calculator calculator = new Calculator();

    @Test
    public void testAdd() {
        assertEquals(3, calculator.add(1, 2));
        assertEquals(4, calculator.add(-1, 3));
        assertEquals(0, calculator.add(-1, -1));
    }

    @Test
    public void testDivide() {
        assertEquals(2, calculator.divide(4, 2));
        assertThrows(ArithmeticException.class, () -> calculator.divide(4, 0));
        assertThrows(ArithmeticException.class, () -> calculator.divide(-4, 2));
    }
}
```

The `CalculatorTest` class contains two test methods: `testAdd()` and `testDivide()`. Each method tests a different public method of the `Calculator` class (add and divide, respectively). The test methods use JUnit's `assertEquals()`, `assertThrows()` functions to check the expected results and exceptions.