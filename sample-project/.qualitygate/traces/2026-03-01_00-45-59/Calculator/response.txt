 Here is the JUnit 5 test class for the `Calculator` with the specified rules:

```java
package com.example;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class CalculatorTest {
    private Calculator calculator;

    @BeforeEach
    public void setup() {
        calculator = new Calculator();
    }

    @Test
    public void testAdd() {
        assertEquals(3, calculator.add(1, 2));
        assertEquals(-1, calculator.add(-2, 3));
        assertEquals(0, calculator.add(0, 0));
    }

    @Test
    public void testDivide() {
        assertEquals(2, calculator.divide(4, 2));
        assertEquals(-2, calculator.divide(-4, 2));
        assertThrows(ArithmeticException.class, () -> calculator.divide(5, 0));
    }
}
```

This test class initializes a new `Calculator` instance in the `setup()` method and then tests the `add()` and `divide()` methods in separate test methods. The `testAdd()` method verifies that the `add()` method correctly adds two integers, while the `testDivide()` method checks that the `divide()` method returns the correct result when dividing two integers and also throws an `ArithmeticException` when dividing by zero.